{% set name = "NRSS" %}
{% set version = "2025.07.25.01" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/usnistgov/NRSS/archive/{{ version }}.tar.gz
  sha256: af235ae14cc1ec5f9300f5756b279b97224a97e668ce532c12a59debbed052ac

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - pyhyperscattering >=0.2.10
    - numpy
    - scipy
    - scikit-image
    - pandas
    - matplotlib-base
    - h5py
    - xarray
    - bottleneck
    - cyrsoxs
test:
  requires:
    - python {{ python_min }}
    - pip
  imports:
    - NRSS
  commands:
    - pip check

about:
  home: https://github.com/usnistgov/NRSS/
  license: NIST-PD
  license_file: LICENSE
  summary: NIST RSoXS Simulation Suite


extra:
  recipe-maintainers:
    - pbeaucage
    - delongchamp
