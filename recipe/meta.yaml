{% set name = "NRSS" %}
{% set version = "2026.03.15.03" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/usnistgov/NRSS/archive/{{ version }}.tar.gz
  sha256: 6625c9fdd49c278ac48db7f9b1caf418643715d151cd1cf98caa3de71ca5c9f3

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - pyhyperscattering >=0.2.10
    - numpy
    - scipy
    - scikit-image
    - pandas
    - matplotlib-base
    - h5py
    - xarray
    - bottleneck
    - cyrsoxs
test:
  requires:
    - python {{ python_min }}
    - pip
  imports:
    - NRSS
  commands:
    - pip check

about:
  home: https://github.com/usnistgov/NRSS/
  license: NIST-PD
  license_file: LICENSE
  summary: NIST RSoXS Simulation Suite


extra:
  recipe-maintainers:
    - pbeaucage
    - delongchamp
